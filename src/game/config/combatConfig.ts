export const COMBAT_CONFIG = {
  PLAYER: {
    ATTACK: {
      WINDUP: 0.12,    // Seconds before attack hits
      ACTIVE: 0.16,    // Seconds attack deals damage
      RECOVERY: 0.25,  // Seconds before player can move/attack again
      COOLDOWN: 0.10   // Seconds before next attack can start
    }
  }
};
