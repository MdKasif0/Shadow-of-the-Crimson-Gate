import { Hitbox } from './Hitbox';
import { Hurtbox } from './Hurtbox';

export class DamageSystem {
  public update(hitboxes: Hitbox[], hurtboxes: Hurtbox[]): void {
    // Scaffolded for Phase 2 Step 2 (Enemies and Hit Detection)
  }
}
